# Benchmark Repo (Sprint 8 fixture)

A small, isolated, deterministic repository used to prove that the AI Media Factory
agent chain (Planner -> Research -> Coding -> Reviewer -> QA -> Documentation) can
perform a real engineering task through the real Sprint 8 capability layer.

## Task

The `add` function in `src/math.js` is implemented incorrectly (it always returns `0`).
Fix the implementation so the test suite in `tests/math-check.js` passes when run with:

```bash
npm test
```
