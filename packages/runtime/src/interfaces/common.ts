/**
 * Re-exports shared primitive types from @ai-media-factory/shared.
 * ARCHITECTURE ONLY — type declarations, no logic.
 */

export type {
  AgentId,
  EventType,
  Timestamp,
  Uuid,
  Json,
} from "@ai-media-factory/shared";
