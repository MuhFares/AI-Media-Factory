# @ai-media-factory/shared

Shared TypeScript types and utilities consumed by the apps in this monorepo.

## Build

```bash
npm run build --workspace packages/shared
```

## Exports

- `MediaKind`, `JobStatus`, `MediaJob` - core domain types
- `greet(name)` - sample utility
