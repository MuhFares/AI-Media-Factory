{
  "name": "@ai-media-factory/brand-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Brand Agent â€” a production quality gate. Consumes an SEO artifact (and its writer lineage) through the normal collaboration handoff and returns an approved / needs_revision / rejected brand review report.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test"
  },
  "dependencies": {
    "@ai-media-factory/runtime": "*",
    "@ai-media-factory/planner-agent": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}