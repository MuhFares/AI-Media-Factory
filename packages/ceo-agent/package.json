{
  "name": "@ai-media-factory/ceo-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Executive decision layer. Produces deterministic ExecutiveDirectives for the Orchestrator. Decisions only — never executes agents, capabilities, or tools.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@ai-media-factory/shared": "*",
    "@ai-media-factory/orchestrator": "*",
    "@ai-media-factory/workflow-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}