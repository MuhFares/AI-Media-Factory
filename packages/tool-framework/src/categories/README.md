# Categories

> Contracts only — declarations, no logic.

| File | Defines | Req |
|---|---|---|
| `categories.ts` | `ToolCategory`, `CategoryConfig`, `CategoryRegistry`, default configs | #3 |

14 built-in categories with default configs for timeout, retry, approval, sandbox level, and cost estimates. Extensible via `CategoryRegistry`.