{
  "name": "@ai-media-factory/provider-adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Real provider adapters wired into the capability framework: web search (Brave), image generation (OpenAI Images), video generation (Replicate), publishing (YouTube Data API v3), analytics (YouTube Analytics v2). Env-configured, timeout/retry, response validation.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "@ai-media-factory/tool-framework": "*",
    "@ai-media-factory/runtime": "*",
    "@ai-media-factory/database": "*"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.5.0"
  }
}
