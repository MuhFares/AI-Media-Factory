{
  "name": "@ai-media-factory/context-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Context Engine — decides exactly what context an agent receives before every execution. Minimizes tokens while maximizing decision quality. Contracts and architecture only.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@ai-media-factory/memory-engine": "*",
    "@ai-media-factory/workflow-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}