# AI Media Factory - Web

TypeScript application that consumes the AI Media Factory API and shared
package.

## Setup

From the repository root (npm workspaces):

```bash
npm install
```

## Develop

```bash
npm run dev --workspace apps/web
```

## Build

```bash
npm run build --workspace apps/web
```
